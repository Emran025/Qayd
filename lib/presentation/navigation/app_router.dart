/// Route path segments for deep linking / consistency (Navigator uses builder + route objects).
abstract final class AppRouter {
  static const String home = '/';
  static const String accounts = '/accounts';
}
